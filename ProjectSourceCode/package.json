{
  "name": "parking-app",
  "version": "1.0.0",
  "description": "Parking application",
  "main": "src/index.js",
  "scripts": {
    "prestart": "npm install",
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "mocha test",
    "testandrun": "npm run prestart && npm run test && npm start",
    "seed_lots": "docker-compose exec -T db sh -c 'psql -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\" -f /docker-entrypoint-initdb.d/01_insert.sql'",
    "render-build": "npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-handlebars": "^7.1.2",
    "express-session": "^1.17.3",
    "pg": "^8.11.3",
    "pg-promise": "^10.11.1"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "chai-http": "^4.3.0",
    "mocha": "^10.2.0",
    "nodemon": "^3.0.1"
  }
}
